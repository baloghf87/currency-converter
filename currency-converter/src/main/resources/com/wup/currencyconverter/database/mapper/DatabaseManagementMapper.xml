<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.wup.currencyconverter.database.mapper.DatabaseManagementMapper">

    <select id="currencyTableCount" resultType="int">
      SELECT COUNT(*) FROM user_tables WHERE table_name='CURRENCY'
    </select>

</mapper>